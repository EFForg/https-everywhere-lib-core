[package]
name = "https-everywhere-lib-core_macros"
version = "0.1.0"
authors = ["William Budington <bill@eff.org>"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = ["full"] }
quote = "1.0"
